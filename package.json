{
  "name": "shipcheck-demo-ai-app",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny demo repo showing Shipcheck findings and SARIF upload for AI-built apps.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.43.0",
    "openai": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  },
  "packageManager": "npm@11.8.0",
  "license": "MIT"
}
